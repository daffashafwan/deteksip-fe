import React from 'react';
import { ReactDOM } from 'react';
import Landing from '../Landing';
import { render } from '@testing-library/react';

it("landing render dengan mulus", () =>{
    render(<Landing></Landing>);
})

